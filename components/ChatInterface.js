// React imports
import React, { useState, useEffect } from 'react';

// Networking Imports
import { fetchEventSource } from '@microsoft/fetch-event-source';

const ChatInterface = ({ role, onChangeRole }) => {
    
};

export default ChatInterface;